#include <stdio.h>  
void main()  
{  
    int i, j, r,k;  
    printf (" Enter a number to define the rows: \n ");  
    scanf("%d", &r);  
    printf("\n");
    for(i=1;i<=r;i++)
    {
        for(j=i;j<r;j++)
        {
            printf(" ");
        }
        for(k=1;k<=i;k++)
        {
            printf("*");
        }
        printf("\n");
    }
}
